class NoteItem
{
    constructor(title, headline, content, toolTipText)
    {
        this.title = title;
        this.toolTipText = toolTipText;
        this.headline = headline;
        this.content = content;
    }
}

export default NoteItem
